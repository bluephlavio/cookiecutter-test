{
  "title": "problemset",
  "slug": "{{ cookiecutter.title | lower | replace(' ', '-') }}",
  "dir": "{{ cookiecutter.slug }}",
  "_copy_without_render": [
    "templates/*.tmpl",
    "tasks.py"
  ]
}
